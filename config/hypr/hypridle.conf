general {
    lock_cmd = pidof hyprlock || /home/dln/.dotfiles/bin/lock       # avoid starting multiple hyprlock instances.
    before_sleep_cmd = loginctl lock-session    # lock before suspend.
    after_sleep_cmd = hyprctl dispatch 'hl.dsp.dpms({ action = "enable" })'  # to avoid having to press a key twice to turn on the display.
    #on_unlock_cmd = sleep 3 && swayosd-client --custom-message="welcome back"
}
listener {
    timeout = 300                                 # 5min
    on-timeout = loginctl lock-session            # lock screen when timeout has passed
    condition_cmd = /home/dln/.dotfiles/bin/not-using-lg-monitor.sh
    condition_retry = 30
}

listener {
    timeout = 1800                                                                                  # 5.5min
    on-timeout = hyprctl dispatch 'hl.dsp.dpms({ action = "disable" })'                            # screen off when timeout has passed
    on-resume = hyprctl dispatch 'hl.dsp.dpms({ action = "enable" })' && brightnessctl -r          # screen on when activity is detected after timeout has fired.
    condition_cmd = /home/dln/.dotfiles/bin/not-using-lg-monitor.sh
    condition_retry = 30
}
listener {
    timeout = 10800 # 3h
    on-timeout = systemctl suspend                # suspend pc
    condition_cmd = /home/dln/.dotfiles/bin/not-using-lg-monitor.sh
    condition_retry = 30
}

