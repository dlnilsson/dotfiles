#!/usr/bin/env bash


current=$(setxkbmap -print | awk -F"+" '/xkb_symbols/ {print $2}')
case $current in
  "se")
    setxkbmap eurkey
    ;;
  "eurkey")
    setxkbmap se
    ;;

  *)
    echo "noop current is $current"
    ;;
esac
